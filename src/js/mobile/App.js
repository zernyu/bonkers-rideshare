'use strict';

var React = require('react');
var Events = require('../components/Events/Events');

var App = React.createClass({
  render: function () {
    return <Events/>
  }
});

module.exports = App;
